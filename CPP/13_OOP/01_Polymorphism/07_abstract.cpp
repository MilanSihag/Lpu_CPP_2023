#include<iostream>
using namespace std;
/*
	Abstract class:
	refers to class containing atleast one pure virtual function, which cannot be instantiated.
	characteristics:
	- abstract class must have at least one pure virtual function.
	- abstract class cannot be instantiated but pointer and refernces of class types can be created
	- we cannot create object of abstract class
	- class that inherit the abstract class must implement all pure virtual function
*/
int main(){
	
}
